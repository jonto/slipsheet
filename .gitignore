node_modules/
build/

# Internal planning, host-integration, and partner-coordination notes.
# Never published — see docs/internal/PLANNING.md.
docs/internal/

# Design context for the impeccable skill. Scaffolding for future design work,
# not documentation for adopters.
PRODUCT.md
DESIGN.md

# Local task board. Project management is the maintainer's business, not the
# adopter's; contributors use GitHub Issues.
kanban/
KANBAN.md

# Note: packages/*/dist/ is intentionally NOT gitignored.
# Committed so jsdelivr can serve the built bundle from the GitHub repo before
# the first npm publish, and so examples/dist.html works without requiring a
# build step in the demo path. Reconsider after first npm publish.
*.log
.DS_Store
.env
.env.local
coverage/
.nyc_output/
*.tsbuildinfo
.vscode/
.idea/
